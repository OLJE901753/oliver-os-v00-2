#!/usr/bin/env node
/**
 * BMAD CLI Entry Point
 * Enhanced BMAD CLI with full functionality
 */

import { EnhancedBMADCLI } from './cli';

// Create and run the enhanced CLI
const cli = new EnhancedBMADCLI();
cli.run();

